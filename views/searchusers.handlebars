<h1>Search User</h1>
{{#if error}} <span>{{error}}</span> {{/if}}
<form class="form-inline" method="POST" action="/user/search">
    <div class="form-group">
        <input class="form-control-plaintext" name="id" type="text" placeholder="Search user by ID">
    </div>
    <button type="submit" class="btn btn-primary" value="Search">Submit</button>
</form>